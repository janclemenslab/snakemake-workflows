name: pb_speed
description: Compute stimulus-aligned speed traces from merged SLEAP tracks and save them as NPZ files.
input:
  - '*_sleap.h5': merged full-frame SLEAP tracks (h5)
output:
  - '*_spd.npz': stimulus-aligned speed traces and DAQ metadata (npz)
